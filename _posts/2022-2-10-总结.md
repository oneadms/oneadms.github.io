---
layout: post
title: 2022.2.10总结
tag: 
   - java
   - java基础
---



2022/2/10 总结

进制转换

> 1. 常见的进制: 二进制 八进制 十进制 十六进制
>
> 2. 其他进制转换为十进制:  基数*系数的权次幂
>
> 3. 解释:
>
>    - 基数：数据的每一位
>    - 系数； 进制数
>    - 权次幂：是基数的每一位 从后往前开始算 从0开始
>
>    进制的标识符:
>
>    0b 二进制
>
>    0 八进制
>
>    十进制无符号
>
>    0x(X) 十六进制

位运算

```
左位移 << 数据会矿大两倍
右位移 >> 数据会缩小为原来的2/1
无符号右位移 >>> 数据会缩小为原来的2/1 只针对于正整数 效率高于右位移
```

代码

```java
public class Test02 {
    public static void main(String[] args) {
        //向左位移
        int num1=10;
        int sum = num1 <<1; // num*2^1
        System.out.println(sum);
        int sum01 = num1 <<2; //num*2^2
        System.out.println(sum01);

        //向右位移
        int num2= -8;
        int sum02= num2>>1; //num2*2^-1
        System.out.println(sum02);
        int sum03 =num2>>2; //num2*2^-2
        System.out.println(sum03);

        int sum04 = num2 >>>2;
        System.out.println(sum04);

    }
}
```

Scanner

```
1. 实例化对象
2. 调用方法获取数据 如 next() nextInt()

```

流程控制

```java
1.语法:
  if(表达式 ) {
      执行操作 
  }
2.说明:
   A.if 如果 是否  是java中关键字
   B.表达式的返回值类型必须是boolean类型
3.需求:新财如果考试大于80 老爸奖励一个充气娃娃
4.嵌套:
    if(是否考试大于80) {
        老爸奖励一个充气娃娃
    }
5.执行的流程
   A.计算表达式 返回值是boolean  要么true   要么是false
   B.如果返回值为true ==>执行操作
   C.如果返回值为false==>不执行操作

```



if else



```
1.语法:
  if(表达式) {
      执行的操作1
  
  }else{
     执行的操作2
  }
2.说明: 
   A.else 其它 否则  也是java中的关键字
   B.所有的括号都是成对出现的
3.案例: 新财如果考试大于80 老爸奖励一个波多老师充气娃娃 否则大嘴巴
4.嵌套:
    if(考试分数是否大于80) {
            波多老师充气娃娃
    }else{
           大嘴巴
    }
 5.执行的流程
    A.计算表达式  表达式返回值类型是boolean
    B.如果表达式的返回值是true  则执行操作1
    C.如果表达式的返回值是false 则执行操作2
 
```

7.6 多分支if

```
语法:
   if(表达式) {
       执行操作1
   }else if(表达式) {
       执行操作2
   }else if(表达式3) {
       执行操作3
       ......
   }else{
       执行操作n
   }
执行流程: 一次对表达式进行判断 如果表达式返回true 则执行对应操作  如果表达式都不满足判断条件 则执行else
说明:
   A.表达式的条件 一般是从小到大的范围 或者从大到小的范围进行排列 不能随意切换
   B.多分支if一般用于区间进行判断
   C.else可以省略 如果省略 可能出现什么操作都不执行
         
```

 嵌套if

```
1.概念:一个完整的if选择结构 嵌套另一个完整if选择结构
2.语法:
    if(表达式 A) {
        if(表达式 B) {
             执行的操作 C
        }
    }
3.案例:何海鹏参加100米选拔比赛  100m的成绩必须在11秒以内  判断是进入男子组 女子组里参加比赛
4.嵌套：
   if(成绩<11){
       if(判断性别){
           男子组
       }else {
           女子组
       }
   }
5.执行流程:
    计算表达式A 
        ==>如果返回值为false  不执行任何操作
        ==>如果返回值为true   计算表达式B
            ==>如果为false  不执行操作
            ==>如果为true   则执行对应的操作
 6.说明:
    外层if表达式的返回值为true 才可能执行内层if选择结构的操作
```

switch

```
1.语法：
  switch (表达式) {
      case 常量1:
           执行操作1
        break;
      case 常量2:
           执行操作2
        break;
      case  常量3:
           执行操作3
        break;
        .....
      default
          执行操作n
        break; 
  }
2.单词:  switch 开关  case 情况  break 中断    default 默认 
3.案例:使用swtich 统计这一周的伙食
4.执行的流程:
   A.计算表达式 
   B.依次与常量进行匹配  如果可以匹配 执行对应的操作
   C.如果不可以匹配 则执行default操作
5.说明:
    A.表达式的类型
       在jdk1.5之后可以使用 short  byte  char  int  
       在jdk1.7之后 可以使用String类型
       总结:目前使用的jdk1.8 所有以上类型都可以使用
    B.case 后必须是常量  每一个常量必须是唯一的
    C.case 一般是与break进行搭配使用 如果break 会继续往下执行 直到遇到break才结束
    D.default 可以省略   有可能产生什么操作都不执行
```

for循环

```
1.语法:
   for(初始化语句 A ①; 循环条件判断B②; 循环变量的改变C ④) {
       循环操作D ③
   }
2.例子:吴启龙每天跑步1200m  标准跑道400m   每天需要跑步1200/400=3
3.分析:
     从第一圈开始跑步  初始化语句  int i=1
       是否需要继续跑步 循环条件判断 i<=3
           继续跑步 循环操作 sout("跑步")
           心里默默进行累加 循环变量的改变 i++
        停止跑步 结束循环 i=4 
 4.执行的流程:  
    执行初始化语句 A(i=1)==>循环条件判断B(i<=3) ==>循环操作D==> 循环变量改变 C(i=2)
    ==>循环条件判断B(i<=3)==>循环操作D==>循环变量的改变C(i=3)
    ==>.......
    ==>直到 i=4 结束循环
 5.说明:
    A.初始化语句只执行一次
    B.循环操作 0   -- n   满足循环条件判断的次数
    C.注意循环变量的改变 避免死循环
    D.循环操作的语句可以是多条
```







